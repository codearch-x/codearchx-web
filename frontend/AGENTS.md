## Build setup

- Always use `pnpm` as package manager

## Implementation

### React
- Try to use ready components from the React libraries
- [IMPORTANT] Avoid writing CSS as much as possible, try using the provided components

### TypeScript
- Do not use `function` use `const` when declaring functions
